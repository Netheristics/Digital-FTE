[project]
name = "digital-fte"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-generativeai>=0.8.6",
    "litellm>=1.98.0",
    "openai-agents[sqlalchemy]>=0.20.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
digital-fte = "digital_fte:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
